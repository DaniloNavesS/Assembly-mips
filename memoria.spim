
    li $t5, 4

    li $v0, 5
    syscall
    move $t0, $v0           # Valor de i

    li $v0, 5
    syscall
    move $t1, $v0           # Valor de j

    li $v0, 5
    syscall
    move $t2, $v0           # Valor de N

    mul $t0, $t0, $t5
    mul $t1, $t1, $t5

    lw $t3, $t1($s0)        # Pega valor de A[j] e adiciona em $t3
    add $s3, $t3, $t2       # Soma N ao $t3
    sw $s3, $t0($s0)        # Adiciona o resultado em A[i]

    li $v0, 10
    syscall